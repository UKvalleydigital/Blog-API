const Post = function () {
    let postList = [];

    const createPost = () => {
        const list = document.createElement('ul');
        const point = document.createElement('li');

        // Fetch input post data

    };
};